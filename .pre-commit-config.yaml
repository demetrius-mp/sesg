fail_fast: true
repos:
  - repo: local
    hooks:
      - id: format
        name: Formats the package using ruff, docformatter, toml-sort, and black.
        entry: poetry run task fmt
        language: system
        types: [python]
